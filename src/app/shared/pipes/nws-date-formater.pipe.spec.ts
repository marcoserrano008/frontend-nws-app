import { NwsDateFormaterPipe } from './nws-date-formater.pipe';

describe('NwsDateFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new NwsDateFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
